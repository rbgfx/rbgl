version: 2
updates:
  # Bundler cannot fetch the sibling path dependencies in this repository.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
