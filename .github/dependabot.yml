version: 2
updates:
  # Bundler updates cannot resolve the sibling path gems in the isolated updater.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
